"use strict";

/* Firebase configuration for the (future) Abodes leaderboard.
 *
 * Leaderboard work comes later. Until a real config is filled in here, the
 * leaderboard stays unconfigured and the app runs fully offline/local. To
 * enable it later, drop your Firebase web config into this object. */
window.AbodesFirebaseConfig = null;
